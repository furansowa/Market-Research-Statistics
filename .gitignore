# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environment
.venv/

# Generated/local data — regenerated from the raw CSVs via `python run_etl.py`
# (db is several GB, far past what should ever go into git)
data/

# Local reference material (e.g. copyrighted PDFs) — not project source
doc/

# Local Claude Code settings (machine-specific tool permissions, not shared config)
.claude/settings.local.json

# OS cruft
.DS_Store
Thumbs.db
